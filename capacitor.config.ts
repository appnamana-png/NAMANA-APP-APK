import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.namana.physio',
  appName: 'Namana Physiotherapy Clinic',
  webDir: 'dist'
};

export default config;
